server:
  port: 9090

spring:
  application:
    name: HardisHard
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/hardishard
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "ThisIsASecretKeyThatIsLongEnoughToWork!!"
  expirationMs: 86400000